apiVersion: v1
kind: Secret
metadata:
  name: smart-bet-secret
  labels:
    app: smart-bet-backend
    environment: dev
type: Opaque
stringData:
  # ============================================================
  # ATENÇÃO: NÃO COMMITAR VALORES REAIS!
  # 
  # Crie a secret manualmente via kubectl:
  #
  # kubectl create secret generic smart-bet-secret \
  #   --from-literal=db-user=doadmin \
  #   --from-literal=db-password=SUA_SENHA_REAL \
  #   --from-literal=jwt-secret=$(openssl rand -base64 32) \
  #   -n smartbet-dev \
  #   --dry-run=client -o yaml | kubectl apply -f -
  #
  # ============================================================
  db-user: "CHANGE_ME"
  db-password: "CHANGE_ME"
  jwt-secret: "CHANGE_ME"
