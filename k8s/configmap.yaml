apiVersion: v1
kind: ConfigMap
metadata:
  name: smart-bet-config
  labels:
    app: smart-bet-backend
data:
  # Database configuration
  # Update these values for your DigitalOcean Managed Database
  db-host: "your-postgres-host.db.ondigitalocean.com"
  db-port: "25060"
  db-name: "smartbet"
  
  # Application configuration
  spring-profile: "docker"
  log-level: "INFO"
